#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    char a;
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */  
    scanf("%s",&a);
    printf("%s",a);
    char b;
    scanf("%s",&b);
    printf("%s",b);
    char c;
    scanf("%s",&c);
    printf("%s",c);
    return 0;
}